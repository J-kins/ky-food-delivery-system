<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /public/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  RewriteRule ^api/(.*)$ /api/index.php?path=$1 [QSA,L]
  RewriteRule ^(.*)$ /public/index.html?path=$1 [QSA,L]
</IfModule>

<FilesMatch "^\.">
  Deny from all
</FilesMatch>
