<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /delivery/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  RewriteRule ^(.*)$ /delivery/index.html [QSA,L]
</IfModule>

<FilesMatch "^\.">
  Deny from all
</FilesMatch>
